import 'package:flutter/material.dart';

class CustomerManageScreen extends StatelessWidget {
  const CustomerManageScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('고객 관리 화면'));
  }
}
