import 'package:flutter/material.dart';

class ProductManageScreen extends StatelessWidget {
  const ProductManageScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('상품 관리 화면'));
  }
}
