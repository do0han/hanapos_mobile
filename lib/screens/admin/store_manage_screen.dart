import 'package:flutter/material.dart';

class StoreManageScreen extends StatelessWidget {
  const StoreManageScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('매장/직원 관리 화면'));
  }
}
